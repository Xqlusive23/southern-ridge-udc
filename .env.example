NEXT_PUBLIC_SMARTSUPP_KEY=
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=
SMTP_FROM=noreply@southernridgeudc.org
SMTP_SECURE=true
SESSION_SECRET=

# Railway: attach a volume mounted at /data, then set:
DATA_DIR=/data

# Optional — only needed on Vercel (ephemeral disk). Not required on Railway.
BLOB_READ_WRITE_TOKEN=
